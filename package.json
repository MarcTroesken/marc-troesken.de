{
  "name": "marc-troesken-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "typecheck": "nuxt typecheck",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "ci:quality": "npm run typecheck && npm run build && npm run generate && npm run audit:prod",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxtjs/sitemap": "^7.6.0",
    "nuxt": "^4.3.1",
    "vue": "^3.5.28"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.2.0",
    "@nuxtjs/tailwindcss": "^6.12.0",
    "@types/node": "^25.2.3",
    "typescript": "^5.6.0"
  }
}
